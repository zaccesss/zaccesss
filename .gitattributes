# The SVGs in this repo are generated by isaacadjei.py, not authored by hand. Marking
# them generated keeps GitHub's language bar on Python rather than SVG and collapses
# the noisy per-build SVG diffs in reviews.
*.svg linguist-generated=true
